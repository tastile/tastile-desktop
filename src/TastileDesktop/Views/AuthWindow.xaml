<Window
    x:Class="TastileDesktop.Views.AuthWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TastileDesktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sign in to Tastile">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border x:Name="TitleBarArea" Margin="12,12,12,0" Padding="14,10" CornerRadius="20" Background="{StaticResource AppSurfaceElevatedBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1">
            <TextBlock Text="Authentication" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource AppForegroundBrush}" />
        </Border>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <ProgressRing IsActive="True" Width="48" Height="48"/>
            
            <TextBlock 
                Text="Waiting for authentication..." 
                HorizontalAlignment="Center"
                Foreground="{StaticResource AppForegroundBrush}"/>
            
            <TextBlock 
                Text="Please complete sign-in in your browser" 
                HorizontalAlignment="Center"
                Foreground="{StaticResource AppForegroundMutedBrush}"/>
            
            <TextBlock 
                x:Name="StatusTextBlock"
                Text=""
                HorizontalAlignment="Center"
                TextWrapping="Wrap"
                Foreground="{StaticResource AppForegroundMutedBrush}"/>
            
            <Button 
                x:Name="CancelButton"
                Content="Cancel" 
                Click="OnCancelClick"
                HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
