<Window
    x:Class="TastileDesktop.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sign in to Tastile">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarArea" Height="32" Padding="16,0,12,0" Background="Transparent">
            <TextBlock Text="Sign in" Style="{ThemeResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" Foreground="{StaticResource AppForegroundBrush}" />
        </Grid>

        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Padding="{StaticResource WindowPagePadding}">
                    <Border Style="{StaticResource WindowCardBorderStyle}" Padding="24" MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel Spacing="20">
                            <StackPanel Spacing="6">
                                <TextBlock Text="Use your Tastile account" Style="{StaticResource PageTitleTextStyle}" />
                                <TextBlock Text="Sign in with email and password. The desktop app restores this session on the next launch." Style="{StaticResource SectionBodyTextStyle}" />
                            </StackPanel>

                            <Border x:Name="ErrorBorder" Background="{StaticResource AppSurface2Brush}" BorderBrush="#FFD13438" BorderThickness="1" CornerRadius="0" Padding="12" Visibility="Collapsed">
                                <TextBlock x:Name="ErrorTextBlock" Foreground="#FFD13438" TextWrapping="Wrap" />
                            </Border>

                            <StackPanel Spacing="12">
                                <TextBox x:Name="EmailTextBox" Header="Email" PlaceholderText="you@example.com" InputScope="EmailSmtpAddress" KeyDown="OnEmailKeyDown" />
                                <PasswordBox x:Name="PasswordBox" Header="Password" PlaceholderText="Enter your password" KeyDown="OnPasswordKeyDown" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                                <Button Content="Cancel" Click="OnCancelClick" />
                                <Button x:Name="SignUpButton" Content="Create account" Click="OnSignUpClick" />
                                <Button x:Name="SignInButton" Content="Sign in" Style="{StaticResource AccentButtonStyle}" Click="OnSignInClick" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>

            <Grid x:Name="LoadingOverlay" Background="#66000000" Visibility="Collapsed">
                <Border Style="{StaticResource WindowCardBorderStyle}" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel Spacing="12">
                        <ProgressRing IsActive="True" Width="40" Height="40" HorizontalAlignment="Center" />
                        <TextBlock Text="Signing in..." HorizontalAlignment="Center" Foreground="{StaticResource AppForegroundBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
