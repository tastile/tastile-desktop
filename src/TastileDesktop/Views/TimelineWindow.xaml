<Window
    x:Class="TastileDesktop.Views.TimelineWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TastileDesktop.Views"
    xmlns:viewModels="using:TastileDesktop.ViewModels"
    x:Uid="Timeline"
    x:DataType="local:TimelineWindow"
    Title="Timeline">
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBarArea" Height="32" Padding="16,0,12,0" Background="Transparent">
            <TextBlock x:Uid="Timeline_TitleBarHeading" Text="Timeline" Style="{ThemeResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" Foreground="{StaticResource AppForegroundBrush}" />
        </Grid>

        <Grid x:Name="TimelineRootGrid" Grid.Row="1" Padding="{StaticResource WindowPagePadding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border x:Name="ToolbarPanel" Grid.Row="0" Padding="12" Background="{StaticResource AppSurface1Brush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" CornerRadius="10">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" x:Uid="Timeline_Previous" Content="←" MinWidth="36" />
                    <Button Grid.Column="1" x:Uid="Timeline_Today" Content="{x:Bind ViewModel.TimelineAnchorLabel, Mode=OneWay}" ToolTipService.ToolTip="現在時点へ戻る" MinWidth="128" />
                    <Button Grid.Column="2" x:Uid="Timeline_Next" Content="→" MinWidth="36" />

                    <Button Grid.Column="4" x:Uid="Timeline_Day" Content="Day" />
                    <Button Grid.Column="5" x:Uid="Timeline_Week" Content="Week" />
                    <Button Grid.Column="6" x:Uid="Timeline_Month" Content="Month" />
                    <Button Grid.Column="7" x:Uid="Timeline_Year" Content="Year" />

                    <ComboBox Grid.Column="9" MinWidth="84" />
                    <Button Grid.Column="10" x:Uid="Timeline_ZoomOut" Content="-" MinWidth="32" />
                    <Button Grid.Column="11" x:Uid="Timeline_ZoomIn" Content="+" MinWidth="32" />

                    <TextBlock Grid.Column="12" Text="{x:Bind ViewModel.TimelineCompactRangeLabel, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource SectionBodyTextStyle}" Foreground="{StaticResource AppForegroundMutedBrush}" />
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,12,0,0">
                <ScrollViewer x:Name="TimelineScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PointerWheelChanged="OnTimelineWheelChanged" Visibility="{x:Bind ViewModel.TimelineCanvasVisibility, Mode=OneWay}">
                    <StackPanel Spacing="12" HorizontalAlignment="Stretch">
                        <Grid x:Name="TimelineCanvasHost" Margin="0,4,0,0" Height="{x:Bind ViewModel.TimelineCanvasHeight, Mode=OneWay}" HorizontalAlignment="Stretch">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.TimelineHourMarkers, Mode=OneWay}" Height="{x:Bind ViewModel.TimelineCanvasHeight, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:TimelineHourMarkerViewModel">
                                        <Grid Width="{Binding ActualWidth, ElementName=TimelineCanvasHost}">
                                            <Grid.RenderTransform>
                                                <TranslateTransform Y="{x:Bind Top, Mode=OneTime}" />
                                            </Grid.RenderTransform>
                                            <Rectangle Width="{Binding ActualWidth, ElementName=TimelineCanvasHost}" Height="1" Fill="{StaticResource AppBorderBrush}" />
                                            <TextBlock Text="{x:Bind Label, Mode=OneTime}" Foreground="{StaticResource AppForegroundMutedBrush}" FontSize="10" Margin="4,-14,0,0" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <ItemsControl ItemsSource="{x:Bind ViewModel.TimelineBlocks, Mode=OneWay}" Height="{x:Bind ViewModel.TimelineCanvasHeight, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:TimelineAbsoluteBlockViewModel">
                                        <Border Width="{x:Bind Width, Mode=OneTime}" Height="{x:Bind Height, Mode=OneTime}" Padding="10,6" CornerRadius="6" Background="{x:Bind Fill, Mode=OneTime}" BorderBrush="{x:Bind BorderBrush, Mode=OneTime}" BorderThickness="1">
                                            <Border.RenderTransform>
                                                <TranslateTransform X="{x:Bind Left, Mode=OneTime}" Y="{x:Bind Top, Mode=OneTime}" />
                                            </Border.RenderTransform>
                                            <Grid ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Button Grid.Column="0" Width="28" Height="28" Padding="0" Margin="0,0,2,0" Background="Transparent" BorderThickness="0" Command="{x:Bind StatusCommand, Mode=OneTime}">
                                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind StatusIconGlyph, Mode=OneTime}" Foreground="{x:Bind StatusForegroundBrush, Mode=OneTime}" FontSize="14" VerticalAlignment="Center" />
                                                </Button>
                                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                                    <TextBlock Text="{x:Bind Title, Mode=OneTime}" Foreground="{x:Bind ForegroundBrush, Mode=OneTime}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock Text="{x:Bind TimeRangeText, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" FontSize="12" />
                                                    <TextBlock Text="{x:Bind KindLabel, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" FontSize="11" Visibility="{x:Bind KindLabelVisibility, Mode=OneTime}" />
                                                </StackPanel>
                                                <Button Grid.Column="2" Width="28" Height="28" Padding="0" Margin="0,0,4,0" Background="Transparent" BorderThickness="0" Command="{x:Bind EditCommand, Mode=OneTime}">
                                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE70F;" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" FontSize="13" VerticalAlignment="Center" />
                                                </Button>
                                                <TextBlock Grid.Column="3" Text="{x:Bind DurationText, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Canvas Width="{Binding ActualWidth, ElementName=TimelineCanvasHost}" Visibility="{x:Bind ViewModel.TimelineNowVisibility, Mode=OneWay}">
                                <Canvas.RenderTransform>
                                    <TranslateTransform Y="{x:Bind ViewModel.TimelineNowTop, Mode=OneWay}" />
                                </Canvas.RenderTransform>
                                <Rectangle Canvas.Top="0" Width="{Binding ActualWidth, ElementName=TimelineCanvasHost}" Height="2" Fill="{StaticResource AppPrimaryBrush}" />
                                <Ellipse Canvas.Left="-4" Canvas.Top="-3" Width="8" Height="8" Fill="{StaticResource AppPrimaryBrush}" />
                                <TextBlock Canvas.Top="-14" Width="{Binding ActualWidth, ElementName=TimelineCanvasHost}" TextAlignment="Right" Padding="0,0,4,0" Text="{x:Bind ViewModel.TimelineNowLabel, Mode=OneWay}" Foreground="{StaticResource AppPrimaryBrush}" FontSize="11" />
                            </Canvas>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <Grid x:Name="MonthCalendarHost" Visibility="{x:Bind ViewModel.MonthCalendarVisibility, Mode=OneWay}" RowSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" x:Uid="Timeline_WeekdayMon" Text="Mon" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="1" x:Uid="Timeline_WeekdayTue" Text="Tue" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="2" x:Uid="Timeline_WeekdayWed" Text="Wed" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="3" x:Uid="Timeline_WeekdayThu" Text="Thu" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="4" x:Uid="Timeline_WeekdayFri" Text="Fri" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="5" x:Uid="Timeline_WeekdaySat" Text="Sat" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                        <TextBlock Grid.Column="6" x:Uid="Timeline_WeekdaySun" Text="Sun" HorizontalAlignment="Center" Style="{StaticResource SectionBodyTextStyle}" />
                    </Grid>

                    <ItemsControl Grid.Row="1" ItemsSource="{x:Bind ViewModel.MonthCalendarRows, Mode=OneWay}" VerticalAlignment="Stretch">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="viewModels:MonthCalendarRowViewModel">
                                <ItemsControl ItemsSource="{x:Bind Cells, Mode=OneTime}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="viewModels:MonthCalendarCellViewModel">
                                            <Border Tag="MonthCell" Margin="0,0,0,8" Padding="8" Background="{StaticResource AppSurface1Brush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" CornerRadius="8" Opacity="{x:Bind CellOpacity, Mode=OneTime}" VerticalAlignment="Stretch">
                                                <Grid RowSpacing="6">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="*" />
                                                    </Grid.RowDefinitions>
                                                    <TextBlock Grid.Row="0" Text="{x:Bind DayNumber, Mode=OneTime}" Foreground="{StaticResource AppForegroundBrush}" FontWeight="SemiBold" />
                                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                                        <ItemsControl ItemsSource="{x:Bind Entries, Mode=OneTime}">
                                                            <ItemsControl.ItemContainerStyle>
                                                                <Style TargetType="ContentPresenter">
                                                                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                                                                </Style>
                                                            </ItemsControl.ItemContainerStyle>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate x:DataType="viewModels:MonthCalendarEntryViewModel">
                                                                    <Grid Margin="0,0,0,2" Height="24" HorizontalAlignment="Stretch">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="24" />
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <Button Grid.Column="0" Width="24" Height="24" Padding="0" Background="Transparent" BorderThickness="0" Command="{x:Bind StatusCommand, Mode=OneTime}">
                                                                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind StatusIconGlyph, Mode=OneTime}" Foreground="{x:Bind StatusForegroundBrush, Mode=OneTime}" FontSize="11" />
                                                                        </Button>
                                                                        <TextBlock Grid.Column="1" Margin="0,0,4,0" Text="{x:Bind Title, Mode=OneTime}" Foreground="{StaticResource AppForegroundBrush}" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                                                        <TextBlock Grid.Column="2" Margin="0,0,2,0" Text="{x:Bind DurationText, Mode=OneTime}" Foreground="{StaticResource AppForegroundMutedBrush}" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="{x:Bind DurationVisibility, Mode=OneTime}" />
                                                                    </Grid>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </ScrollViewer>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

                <!-- Week Timeline View -->
                <ScrollViewer x:Name="WeekTimelineScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PointerWheelChanged="OnTimelineWheelChanged" Visibility="{x:Bind ViewModel.WeekCalendarVisibility, Mode=OneWay}">
                    <Grid x:Name="WeekTimelineColumnsHost">
                        <Grid.Resources>
                            <Style x:Key="TransparentWeekLaneItemContainerStyle" TargetType="ContentPresenter">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <ItemsControl Grid.Row="0" ItemsSource="{x:Bind ViewModel.WeekTimelineColumns, Mode=OneWay}" Margin="0,0,0,8" ItemContainerStyle="{StaticResource TransparentWeekLaneItemContainerStyle}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="viewModels:TimelineWeekColumnViewModel">
                                    <Grid Width="{Binding DataContext.WeekDayColumnWidth, ElementName=WeekTimelineColumnsHost, Mode=OneWay}" Background="Transparent">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="6">
                                            <TextBlock Text="{x:Bind DayLabel, Mode=OneTime}" Foreground="{StaticResource AppForegroundMutedBrush}" FontSize="11" />
                                            <TextBlock Text="{x:Bind DayNumber, Mode=OneTime}" Foreground="{StaticResource AppForegroundBrush}" FontSize="11" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Grid Grid.Row="1">
                            <ItemsControl ItemsSource="{x:Bind ViewModel.WeekTimelineHourMarkers, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:TimelineHourMarkerViewModel">
                                        <Grid Width="{Binding ActualWidth, ElementName=WeekTimelineColumnsHost}">
                                            <Grid.RenderTransform>
                                                <TranslateTransform Y="{x:Bind Top, Mode=OneTime}" />
                                            </Grid.RenderTransform>
                                            <Rectangle Width="{Binding ActualWidth, ElementName=WeekTimelineColumnsHost}" Height="1" Fill="{StaticResource AppBorderBrush}" />
                                            <TextBlock Text="{x:Bind Label, Mode=OneTime}" Foreground="{StaticResource AppForegroundMutedBrush}" FontSize="10" Margin="4,-14,0,0" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <ItemsControl ItemsSource="{x:Bind ViewModel.WeekTimelineColumns, Mode=OneWay}" ItemContainerStyle="{StaticResource TransparentWeekLaneItemContainerStyle}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="8" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:TimelineWeekColumnViewModel">
                                        <Grid Width="{Binding DataContext.WeekDayColumnWidth, ElementName=WeekTimelineColumnsHost, Mode=OneWay}" Height="{Binding DataContext.WeekCanvasHeight, ElementName=WeekTimelineColumnsHost, Mode=OneWay}" Background="Transparent">
                                            <ItemsControl ItemsSource="{x:Bind Blocks, Mode=OneTime}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <Canvas />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate x:DataType="viewModels:TimelineAbsoluteBlockViewModel">
                                                        <Border Width="{x:Bind Width, Mode=OneTime}" Height="{x:Bind Height, Mode=OneTime}" Padding="10,6" CornerRadius="6" Background="{x:Bind Fill, Mode=OneTime}" BorderBrush="{x:Bind BorderBrush, Mode=OneTime}" BorderThickness="1">
                                                            <Border.RenderTransform>
                                                                <TranslateTransform X="{x:Bind Left, Mode=OneTime}" Y="{x:Bind Top, Mode=OneTime}" />
                                                            </Border.RenderTransform>
                                                            <Grid ColumnSpacing="12">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <Button Grid.Column="0" Width="28" Height="28" Padding="0" Margin="0,0,2,0" Background="Transparent" BorderThickness="0" Command="{x:Bind StatusCommand, Mode=OneTime}">
                                                                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="{x:Bind StatusIconGlyph, Mode=OneTime}" Foreground="{x:Bind StatusForegroundBrush, Mode=OneTime}" FontSize="14" VerticalAlignment="Center" />
                                                                </Button>
                                                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                                                    <TextBlock Text="{x:Bind Title, Mode=OneTime}" Foreground="{x:Bind ForegroundBrush, Mode=OneTime}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                                                    <TextBlock Text="{x:Bind TimeRangeText, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" FontSize="12" />
                                                                    <TextBlock Text="{x:Bind KindLabel, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" FontSize="11" Visibility="{x:Bind KindLabelVisibility, Mode=OneTime}" />
                                                                </StackPanel>
                                                                <TextBlock Grid.Column="2" Text="{x:Bind DurationText, Mode=OneTime}" Foreground="{x:Bind SecondaryForegroundBrush, Mode=OneTime}" VerticalAlignment="Center" />
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Canvas Width="{Binding ActualWidth, ElementName=WeekTimelineColumnsHost}" Visibility="{x:Bind ViewModel.TimelineNowVisibility, Mode=OneWay}" IsHitTestVisible="False">
                                <Canvas.RenderTransform>
                                    <TranslateTransform Y="{x:Bind ViewModel.TimelineNowTop, Mode=OneWay}" />
                                </Canvas.RenderTransform>
                                <Rectangle Canvas.Top="0" Width="{Binding ActualWidth, ElementName=WeekTimelineColumnsHost}" Height="2" Fill="{StaticResource AppPrimaryBrush}" />
                                <Ellipse Canvas.Left="-4" Canvas.Top="-3" Width="8" Height="8" Fill="{StaticResource AppPrimaryBrush}" />
                                <TextBlock Canvas.Top="-14" Width="{Binding ActualWidth, ElementName=WeekTimelineColumnsHost}" TextAlignment="Right" Padding="0,0,4,0" Text="{x:Bind ViewModel.TimelineNowLabel, Mode=OneWay}" Foreground="{StaticResource AppPrimaryBrush}" FontSize="11" />
                            </Canvas>
                        </Grid>
                    </Grid>
                </ScrollViewer>

                <ItemsControl x:Name="YearCalendarHost" Visibility="{x:Bind ViewModel.YearCalendarVisibility, Mode=OneWay}" ItemsSource="{x:Bind ViewModel.YearCalendarRows, Mode=OneWay}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="viewModels:YearCalendarRowViewModel">
                            <ItemsControl ItemsSource="{x:Bind Months, Mode=OneTime}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="10" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:YearCalendarMonthViewModel">
                                        <Border Tag="YearMonthCard" Margin="0,0,0,10" Padding="8" Background="{StaticResource AppSurface1Brush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="8">
                                                <TextBlock Text="{x:Bind Title, Mode=OneTime}" Foreground="{StaticResource AppForegroundBrush}" FontWeight="SemiBold" />
                                                <ItemsControl ItemsSource="{x:Bind Rows, Mode=OneTime}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate x:DataType="viewModels:MonthCalendarRowViewModel">
                                                            <ItemsControl ItemsSource="{x:Bind Cells, Mode=OneTime}">
                                                                <ItemsControl.ItemsPanel>
                                                                    <ItemsPanelTemplate>
                                                                        <StackPanel Orientation="Horizontal" Spacing="2" />
                                                                    </ItemsPanelTemplate>
                                                                </ItemsControl.ItemsPanel>
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate x:DataType="viewModels:MonthCalendarCellViewModel">
                                                                        <Border Tag="YearDayCell" Margin="0,0,0,2" Padding="2" Opacity="{x:Bind CellOpacity, Mode=OneTime}">
                                                                            <TextBlock Text="{x:Bind DayNumber, Mode=OneTime}" Style="{StaticResource SectionBodyTextStyle}" HorizontalAlignment="Center" />
                                                                        </Border>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Grid x:Name="LoadingOverlay" Background="{StaticResource AppSurface0Brush}" Opacity="0.86" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                        <ProgressRing Width="32" Height="32" IsActive="True" Foreground="{StaticResource AppPrimaryBrush}" />
                        <TextBlock x:Uid="Timeline_Loading" Text="Loading calendar..." Foreground="{StaticResource AppForegroundBrush}" />
                    </StackPanel>
                </Grid>

            </Grid>
        </Grid>
    </Grid>
</Window>