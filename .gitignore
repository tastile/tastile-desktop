# Per-machine / IDE / agent tooling
.idea/
.vs/
.claude/
.agents/
.impeccable/
.cursor/
.vscode/

# Local / test artifacts
artifacts/
*.log
.env

# Build outputs
bin/
obj/
publish/

# Windows signing — must never be committed
*.pfx
*.p12
signing.key
signing.cert

# Development overrides
appsettings.Development.json
appsettings.Local.json
